1	DDL	----> CREATE, ALTER, DROP, TRUNCATE, RENAME ✅ (done)
2	Constraints (DDL ka part) ----->	PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE, DEFAULT, AUTO_INCREMENT, CHECK
3	DML ----->	INSERT, UPDATE, DELETE
4	DQL (querying) ---->	SELECT + Filters: WHERE, AND/OR, LIKE, IN, BETWEEN, IS NULL, ORDER BY, LIMIT, DISTINCT, AS
5	DQL (advanced) ---->	JOINs: INNER, LEFT, RIGHT, FULL OUTER
6	DQL (advanced) ---->	Aggregation: COUNT, SUM, AVG, MIN, MAX, GROUP BY, HAVING
7	DQL (advanced) ---->	Subqueries, CASE WHEN
8	Views + Stored Procedures --->	(Views DQL-based hote hain, Procedures DCL/DML combo)
9	TCL	---> COMMIT, ROLLBACK, SAVEPOINT
10	DCL (bonus, agar chahiye) --->	GRANT, REVOKE (real projects mein kam use hota hai, but concept ke liye)