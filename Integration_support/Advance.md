1) Subquery
        - Query ke andar query
        - Complex data check — "jo records system A mein hain but B mein nahi"

2) CASE WHEN
        - Condition based output
        - Status column banana — "success", "failed", "pending" — reports mein kaam aata hai

3) COMMIT / ROLLBACK
        - Safe data changes karna
        - Production mein data fix karte waqt galti mat karo — rollback safety net hai